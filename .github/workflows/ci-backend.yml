# Nombre del Workflow
name: Backend CI

# Disparadores del Workflow
on:
  pull_request:
    branches:
      - main # Se ejecuta en pull requests hacia main
    paths:
      - "src/backend/**"
  workflow_dispatch: # Permite ejecución manual desde la UI de GitHub Actions

# Definición de los trabajos (Jobs)
jobs:
  # -------------------------------------
  # Job de CI (Build, Test, Publish Docker Image)
  # -------------------------------------
  build-test-publish:
    runs-on: ubuntu-latest # Runner a utilizar
    outputs:
      repo_name: ${{ steps.set_outputs.outputs.repo_name }}
      image_tag: ${{ steps.set_outputs.outputs.image_tag }}

    steps:
      # 1. Checkout del código del repositorio
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Configurar el entorno de Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13' # Especificar versión de Python

      # 3. Instalar dependencias de Python
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ./src/backend/requirements.txt # Instalar paquetes listados en requirements.txt

      # 4. Ejecutar Black (Formateador de código) en modo chequeo
      - name: Run Black (Formatter)
        run: black ./src/backend/app --check

      # 5. Ejecutar Pylint (Linter) y guardar reporte
      - name: Run Pylint (Linter)
        run: pylint ./src/backend/app --output-format=text --fail-under=9 > pylint-report.txt || true

      # 6. Ejecutar Flake8 (Linter) y guardar reporte
      - name: Run Flake8 (Linter)
        run: flake8 ./src/backend/app --output-file=flake8-report.txt || true

      # 7. Ejecutar Pruebas Unitarias con Pytest y generar reporte de cobertura
      - name: Run Unit Tests with pytest and Coverage
        run: |
          pytest ./src/backend/tests \
            --cov=./src/backend/app \
            --cov-report=xml:coverage.xml \
            --cov-report=term \
            --ignore=./src/backend/tests/test_acceptance_app.py \
            --ignore=./src/backend/tests/test_smoke_app.py \
            --ignore=./src/backend/tests/test_e2e_routes.py \
            --ignore=./src/backend/tests/test_security_routes.py

      # 7.1. Cargar reportes de cobertura y pruebas unitarias como artefactos
      - name: Upload Test Reports Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-reports
          path: |
            coverage.xml
            htmlcov/
            report.html

      # 9. Ejecutar análisis con SonarCloud
      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Automáticamente proporcionado
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}   # El secreto que creaste

# --- Pasos de Docker (solo en push a main o ejecución manual) ---

      # 9. Configurar QEMU
      - name: Set up QEMU
        if: (github.event_name == 'pull_request' && github.base_ref == 'main') || github.event_name == 'workflow_dispatch'
        uses: docker/setup-qemu-action@v3

      # 10. Configurar Docker Buildx
      - name: Set up Docker Buildx
        if: (github.event_name == 'pull_request' && github.base_ref == 'main') || github.event_name == 'workflow_dispatch'
        uses: docker/setup-buildx-action@v3

      # 11. Iniciar sesión en Docker Hub
      - name: Login to Docker Hub
        if: (github.event_name == 'pull_request' && github.base_ref == 'main') || github.event_name == 'workflow_dispatch'
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 12. Construir y pushear imagen Docker
      - name: Build and push Docker image
        id: docker_build_push
        if: (github.event_name == 'pull_request' && github.base_ref == 'main') || github.event_name == 'workflow_dispatch'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ vars.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}:${{ github.sha }}
            ${{ vars.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 13. Establecer las salidas del job
      - name: Set Job Outputs
        id: set_outputs
        if: (github.event_name == 'pull_request' && github.base_ref == 'main') || github.event_name == 'workflow_dispatch'
        run: |
          echo "repo_name=${{ github.event.repository.name }}" >> $GITHUB_OUTPUT
          echo "image_tag=${{ github.sha }}" >> $GITHUB_OUTPUT
